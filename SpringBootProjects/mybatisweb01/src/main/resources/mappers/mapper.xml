<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mybatis.mapper.MyBatisMapper">
    <select id="select" resultType="String">
        select now() as now
    </select>

    <select id="selectList" resultType="to">
        select deptno, dname, loc
        from dept
    </select>

<!--    <insert id="insert" parameterType="to">-->
<!--        insert into dept-->
<!--        values ( #{deptno}, #{dname}, #{loc} )-->
<!--    </insert>-->

<!--    <update id="updateByDeptno" parameterType="to">-->
<!--        update dept-->
<!--        set dname=#{dname}, loc=#{loc}-->
<!--        where deptno=#{deptno}-->
<!--    </update>-->

<!--    <delete id="deleteByDeptno" parameterType="to">-->
<!--        delete from dept-->
<!--        where deptno=#{deptno}-->
<!--    </delete>-->
</mapper>